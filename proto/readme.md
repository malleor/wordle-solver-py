This directory contains jupyter notebooks for prototyping solvers. To set up
the environment for the notebooks, use the following commands:

```
cd proto
virtualenv .venv
source .venv/bin/activate
pip install jupyter, pandas
jupyter notebook
```
